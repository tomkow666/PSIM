<%= form_for(@shipment_method) do |f| %>
  <% if @shipment_method.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shipment_method.errors.count, "error") %> prohibited this shipment_method from being saved:</h2>

      <ul>
      <% @shipment_method.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
